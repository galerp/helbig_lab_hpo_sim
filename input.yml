#inputs for Peter

output_dir : /Users/Desktop/hpo_sim_gene     # required

patient_phenome : Files/example_phenome.csv    # required

hpo_tree :  Files/HPO_is.a_tree.csv          # required
hpo_ancestor : Files/HPO_Ancestors_Compressed.csv       # required
hpo_path : Files/paths_data_frame.csv      # required

num_of_iterations : 100000 # required default is 100000

variant_file : Files/variant.csv       # required

algorithm : 1    # optional 1 - Resnik default ; 2 - Cube
local_IC : # optional 
patient_hpo_base : # optional - Specify this file if local_IC is given
patient_hpo_prop : # optional - Specify this file if local_IC is given
sim_matrix : # optional - - Specify this file if local_IC and patient_hpo base and prop are given
